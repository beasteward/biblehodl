DOMAIN=community.example.com
COMMUNITY_NAME="My Community"
COMMUNITY_LOGO_URL=
PRIMARY_COLOR=#3b82f6

ADMIN_NPUB=npub1...
ADMIN_EMAIL=admin@example.com

DATABASE_URL=file:/data/app.db

NEXT_PUBLIC_RELAY_URL=wss://relay.community.example.com
NEXT_PUBLIC_BLOSSOM_URL=https://files.community.example.com

# Meeting voice/video (self-hosted LiveKit SFU).
# Generate a key/secret pair: `docker run --rm livekit/livekit-server generate-keys`
NEXT_PUBLIC_LIVEKIT_URL=wss://livekit.community.example.com
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

CPDV_API_URL=https://cpdv-bible-production.up.railway.app
CPDV_API_KEY=

# Web Push notifications (PWA). Generate a keypair once:
#   npx web-push generate-vapid-keys
# Leave blank to disable server-sent push (in-app message notifications still
# work via the service worker as long as the browser grants permission).
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com

SESSION_SECRET=

# Blossom admin dashboard (https://files.{DOMAIN}/admin)
BLOSSOM_ADMIN_USER=admin
BLOSSOM_ADMIN_PASS=
